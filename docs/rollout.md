# Rollout Gates

1. Install the global skill and route coding tasks to it.
2. Onboard a disposable project and prove `doctor` is read-only.
3. Onboard Gongtu without overwriting existing project files.
4. Run an update report against Gongtu remote refs and inspect branch coverage.
5. Start two isolated previews using distinct ports and data namespaces.
6. Wire daily and weekly automation only after artifacts and permissions are reviewed.
7. Use a clean clone on a second machine as the portability acceptance test.
